---
title: Neural-Network Quantum Embedding Solvers for Correlated Materials
title_zh: 关联材料的神经网络量子嵌入求解器
authors: "Agnes Valenti, Ina Park, Antoine Georges, Andrew J. Millis, Olivier Parcollet"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15741v1"
tags: ["query:cmp"]
score: 8.0
evidence: 用于关联材料和莫特相变的神经网络求解器
tldr: 本研究针对关联材料量子嵌入方法（如DMFT）中的计算瓶颈——量子杂质求解器，提出了一种基于神经网络的新型求解器。该模型在合成数据上训练，能够准确学习从杂化函数和局部相互作用到格林函数的映射。实验证明，该方法在处理单轨道和多轨道模型时具有定量准确性，不仅能重现莫特相变和复杂相图，还能模拟真实材料的电子特性，且计算速度提升了数个数量级。
selection_source: fresh_fetch
motivation: 传统的量子杂质求解器是动力学平均场理论（DMFT）等量子嵌入方法中的主要计算瓶颈。
method: 利用在合成、与材料无关的数据上训练的神经网络，学习从杂化函数和局部相互作用到格林函数的映射关系。
result: 该方法成功重现了莫特相变、Hubbard-Kanamori模型的相图以及SrVO3和SrMnO3等真实材料的电子性质，且速度提升了数个数量级。
conclusion: 神经网络求解器在保持准确性的同时极大加速了DMFT计算，为关联材料的高效模拟提供了强有力的新工具。
---

## 摘要
量子杂质求解器是关联材料量子嵌入方法（如动力学平均场理论，DMFT）的计算瓶颈。我们展示了在合成的、与材料无关的数据上训练的神经网络，能够学习从杂化函数和局部相互作用到格林函数的杂质映射，且对模型系统和真实材料均具有定量准确性，为单轨道和多轨道模型提供了快速求解器。与数值受控的量子蒙特卡罗进行的基准测试表明，该方法重现了莫特（Mott）转变、Hubbard-Kanamori 模型的多轨道相图，以及 SrVO$_3$ 和 SrMnO$_3$ 的电子性质。学习到的求解器实现了几个数量级的加速，并可以初始化受控计算，在保持准确性的同时显著加速了 DMFT。

## Abstract
Quantum impurity solvers are the computational bottleneck of quantum embedding approaches to correlated materials, such as dynamical mean-field theory (DMFT). We show that neural networks trained on synthetic, material-agnostic data learn the impurity mapping from hybridization functions and local interactions to Green's functions with quantitative accuracy for both model systems and real materials, providing fast solvers for single- and multi-orbital models. Benchmarks against numerically controlled quantum Monte Carlo show that the method reproduces the Mott transition, multi-orbital phase diagrams of Hubbard-Kanamori models, and the electronic properties of SrVO$_3$ and SrMnO$_3$. The learned solvers achieve orders-of-magnitude speedup and can initialize controlled calculations, dramatically accelerating DMFT while preserving accuracy.

---

## 论文详细总结（自动生成）

这是一份关于论文《Neural-Network Quantum Embedding Solvers for Correlated Materials》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：在研究强关联电子材料时，动力学平均场理论（DMFT）是主流框架。然而，其核心步骤——**量子杂质求解器（Quantum Impurity Solver）**存在严重的计算瓶颈。传统的数值精确方法（如连续时间量子蒙特卡罗 CT-QMC）计算成本极高，限制了其在复杂材料和大规模系统中的应用。
*   **核心问题**：能否利用神经网络（NN）的高效函数近似能力，构建一个既快速又具有普适性的量子杂质求解器，从而加速关联材料的模拟？

### 2. 方法论
*   **核心思想**：将量子杂质求解过程视为一个高维函数映射，输入杂化函数 $\Delta(\tau)$、局部相互作用（$U, J$）和温度（$\beta$），输出格林函数 $G(\tau)$。
*   **关键技术细节**：
    *   **离散莱曼表示（DLR）**：为了处理虚时间轴上的函数，研究者采用了 DLR 压缩技术。这使得输入和输出向量的维度保持在极小规模（$N_\tau < 100$），且能跨温度（$\beta$）泛化。
    *   **合成数据训练（Material-agnostic Data）**：不针对特定材料训练，而是通过随机生成 DLR 系数构造物理上合理的杂化函数，并利用 CT-QMC 生成对应的格林函数标签。
    *   **数据增强策略**：通过 1-2 次简单的 DMFT 迭代（$\Delta = G$）来增强数据集，确保网络能学习到自洽循环中常见的函数特征。
    *   **网络架构**：针对 1、2 轨道使用多层感知机（MLP），针对 3 轨道使用残差网络（ResNet）。
*   **算法流程**：训练好的 NN 求解器（GNet）可以直接嵌入 DMFT 循环，或者作为“加速器”——先用 GNet 快速达到初步收敛，再用 QMC 进行少量迭代精修。

### 3. 实验设计
*   **场景与数据集**：
    *   **单轨道 Hubbard 模型**：在 Bethe 晶格上模拟半填充状态，重点考察莫特（Mott）金属-绝缘体转变及滞后效应。
    *   **双轨道 Hubbard-Kanamori 模型**：研究晶体场分裂和 Hund 耦合下的相图及轨道极化转变。
    *   **真实材料（DFT+DMFT）**：选取了典型的关联金属 $SrVO_3$ 和莫特绝缘体 $SrMnO_3$。
*   **Benchmark（基准）**：以数值精确的连续时间量子蒙特卡罗（CTSEG 和 CTHYB）作为金标准。
*   **对比方法**：对比了纯 NN 求解器结果、纯 QMC 结果以及“NN 初始化 + QMC 精修”的混合方案。

### 4. 资源与算力
*   **训练成本**：论文列出了训练集规模：1 轨道约 1.6万样本，2 轨道约 1.8万，3 轨道约 8.6万。
*   **推理效率**：NN 求解器运行时间通常在 **1 秒以内**，而传统的 CT-QMC 求解器在多轨道情况下通常需要**数小时甚至数天**。
*   **硬件说明**：文中未明确给出具体的 GPU 型号或训练总时长，但强调了 NN 带来的“数个数量级”的加速。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从理想模型到真实材料、从单轨道到多轨道的多种物理场景。
*   **充分性评价**：实验设计较为充分。作者不仅验证了格林函数 $G(\tau)$ 的准确性，还通过 Dyson 方程推导了自能量 $\Sigma(i\omega)$，并成功捕捉到了物理上的相变点（如 Mott 转变点）。
*   **客观性**：通过与 QMC 的直接对比证明了 NN 在其训练参数范围内的定量准确性，并客观指出了 NN 在自能量高频部分可能存在的微小偏差。

### 6. 主要结论与发现
*   **高精度**：在合成数据上训练的 NN 求解器能够以极高的精度预测模型系统和真实材料的电子性质。
*   **显著加速**：NN 求解器将单次迭代时间从小时级降至秒级。在 $SrVO_3$ 的测试中，使用 NN 初始化可使 DMFT 收敛速度提升约一个数量级。
*   **物理一致性**：NN 能够正确识别莫特转变、磁滞现象以及多轨道系统中的轨道极化。

### 7. 优点
*   **泛化能力强**：由于使用了材料无关的合成数据，该求解器具有很强的通用性，无需为每种新材料重新训练。
*   **DLR 的巧妙应用**：解决了虚时间轴数据维度过高和温度依赖的问题。
*   **混合策略（NN+QMC）**：提供了一种实用的工程方案，既利用了 NN 的速度，又保留了 QMC 的精度控制。

### 8. 不足与局限
*   **精度控制缺失**：作为机器学习方法，NN 求解器本身无法像 QMC 那样通过增加采样量来系统性地降低误差。
*   **轨道数限制**：目前仅展示到 3 轨道系统，对于包含更多轨道（如 $f$ 电子系统）或强自旋轨道耦合的系统，计算复杂度和数据需求将呈指数增长。
*   **外推风险**：虽然在训练范围内表现优异，但对于极端参数或训练集未覆盖的特殊杂化函数形状，可能存在预测失效的风险。

（完）
